# Offline provider fixtures

The JSON records here are synthetic examples of documented provider responses,
not live recordings. Tests construct a small two-page PDF in memory using pypdf
so extraction is exercised without a network call or a third-party document.
